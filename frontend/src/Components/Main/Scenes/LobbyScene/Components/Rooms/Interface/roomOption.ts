export interface roomInfoInterface {
    roomInfo: {
        number: number;
        title: string;
        mode: string;
        rounds: number;
        limitTime: number;
        currEntry: number;
        maxEntry: number;
    }
}